Create venv:

cd project/
python3 -m venv venv
source venv/bin/activate

Upgrade pip:

pip install --upgrade pip

Install requirements.txt:

pip install -r requirements.txt

Run:

translate.py "<path_file>"