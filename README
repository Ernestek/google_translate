GETTING STARTED
Create venv:

cd project/
python -m venv venv
Linux:
source venv/bin/activate
Windows:
venv\Scripts\activate

Upgrade pip:
pip install --upgrade pip

Install requirements.txt:
pip install -r requirements.txt

Run:
py translate.py "<path_file>"